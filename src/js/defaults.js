import {
  VERTICAL,
  CONTAINER_MIN_WIDTH,
  CONTAINER_MIN_HEIGHT,
} from './constants';

export default {
  direction: VERTICAL,
  containerMinWidth: CONTAINER_MIN_WIDTH,
  containerMinHeight: CONTAINER_MIN_HEIGHT,
};
